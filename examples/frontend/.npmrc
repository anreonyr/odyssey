# Allow esbuild to download its platform binary at install time.
# Vite uses esbuild for TS/JSX transform; without this, the build
# script doesn't run and Vite can't start.
enable-pre-post-scripts=true
